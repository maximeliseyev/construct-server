# Security audit configuration
# See: https://rustsec.org/

[advisories]
# Vulnerabilities to ignore (with justification)
ignore = [
    # RUSTSEC-2023-0071: rsa 0.9.9 - Marvin Attack timing sidechannel
    # Status: No fix available
    # Risk: LOW - pulled in by sqlx-mysql, we use PostgreSQL only
    # Action: Monitor for updates monthly
    "RUSTSEC-2023-0071",

    # RUSTSEC-2024-0436: paste - proc-macro denial of service
    # Status: No fix available (crate unmaintained)
    # Risk: LOW - build-time only, doesn't affect runtime security
    # Impact: Potential infinite loop during compilation
    # Mitigation: We use specific version pinned by dependencies
    # Action: Monitor for maintained alternative or upstream fix
    "RUSTSEC-2024-0436",

    # RUSTSEC-2025-0134: rustls-pemfile unmaintained
    # Status: Replacement is rustls-pki-types with pem feature
    # Risk: NONE - no vulnerability, crate still functional
    # Blocker: rustls-pki-types 1.14 (pinned by rustls 0.23) lacks pem feature
    # Action: Remove when upgrading rustls stack to version that includes pem support
    "RUSTSEC-2025-0134",

    # RUSTSEC-2026-0097: rand 0.8.x / 0.10.x unsound with custom global logger
    # Status: No patched rand 0.8.x available; fix requires sqlx 0.9+ (not yet released)
    # Risk: NEGLIGIBLE — unsoundness only triggers when a custom global logger itself
    #       calls rand::rng() during initialisation, a pattern we do not use.
    #       rand is pulled in transitively by sqlx-postgres and apns-h2.
    # Action: Remove when sqlx upgrades to a rand version with the fix
    "RUSTSEC-2026-0097",
]

# Don't fail on unmaintained crates (just warn)
informational_warnings = ["unmaintained"]
