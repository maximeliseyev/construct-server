# env.federation-test.example
#
# Copy this file to .env and fill in the values
# for your federation test instance
#
# cp env.federation-test.example .env
# nano .env

# =============================================================================
# Instance Identity
# =============================================================================

# Your federation test domain (must have DNS pointing to this server)
INSTANCE_DOMAIN=test.konstruct.cc

# Base domain for the federation network
FEDERATION_BASE_DOMAIN=konstruct.cc

# =============================================================================
# Security - GENERATE UNIQUE VALUES!
# =============================================================================

# Database password (generate: openssl rand -base64 32)
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_RANDOM_PASSWORD

# JWT secret (generate: openssl rand -base64 64)
JWT_SECRET=CHANGE_ME_GENERATE_RANDOM_SECRET

# Log hash salt for privacy (generate: openssl rand -base64 32)
LOG_HASH_SALT=CHANGE_ME_GENERATE_RANDOM_SALT

# Server signing key for federation (Ed25519 seed, generate: openssl rand -base64 32)
# This key is used to sign S2S messages - KEEP IT SECRET!
SERVER_SIGNING_KEY=CHANGE_ME_GENERATE_RANDOM_KEY

# =============================================================================
# Optional: APNs for push notifications
# =============================================================================

# APNS_KEY_ID=
# APNS_TEAM_ID=
# APNS_PRIVATE_KEY_PATH=
# APNS_DEVICE_TOKEN_ENCRYPTION_KEY=

# =============================================================================
# Quick Start Commands
# =============================================================================

# Generate all secrets at once:
# echo "POSTGRES_PASSWORD=$(openssl rand -base64 32)"
# echo "JWT_SECRET=$(openssl rand -base64 64)"
# echo "LOG_HASH_SALT=$(openssl rand -base64 32)"
# echo "SERVER_SIGNING_KEY=$(openssl rand -base64 32)"
